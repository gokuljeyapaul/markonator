# macOS
.DS_Store

# Node / editor
node_modules/
*.log
package-lock.json

# Playwright
test-results/
playwright-report/

# Generated screenshots (built by tests/screenshots.spec.js + scripts/make-gif.sh).
# The canonical hero PNG + demo GIF live in docs/ and on GitHub releases.
screenshots/

# Generated extension build artifacts (run `python3 build.py` to regenerate).
# The extension's own source (manifest.json, background.js) IS committed;
# these are derived from the shared src/ + vendor/ + icons/ at the repo root.
extension/app.js
extension/styles.css
extension/vendor/
extension/icons/
extension/index.html
extension/sidepanel.html
extension/*.zip
