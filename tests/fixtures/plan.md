# Demo Plan

## Goal

Build a user settings page where users can update display name and theme.

## Steps

1. Create the `SettingsPage` component under `src/pages/`.
2. Add a `settings` route to the router.
3. The theme field is a single string.

## Open Questions

- Should we debounce the save action?